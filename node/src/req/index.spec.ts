import { it } from '../test-runner';
import { strict as assert } from 'node:assert';

it('works', async () => {
  assert.equal(2 + 2, 4);
});

it('works again', async () => {
  assert.equal(2 + 2, 4);
});
