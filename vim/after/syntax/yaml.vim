" by default, this is linked to Identifier,
" which has no color, as configured in vimrc
hi link yamlBlockMappingKey Function

hi! def link yamlDocumentStart Function
hi! def link yamlBlockCollectionItemStart Function
