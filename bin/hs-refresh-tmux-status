#!/usr/bin/env bash

# Note: BREW_PREFIX won't be available, as this is called via hs.task.new
TMUX=/usr/local/bin/tmux
if ! [ -f "$TMUX" ]; then
  TMUX=/opt/homebrew/tmux
fi

"$TMUX" refresh-client -S
